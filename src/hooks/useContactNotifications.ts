// Temporary stub to satisfy type checks during deployment
export function useContactNotifications() {
  return {
    notifications: [],
    unreadCount: 0,
    markAllAsRead: () => {},
    refresh: () => {},
  };
}

